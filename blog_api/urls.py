from django.urls import path

# from .views import PostList, PostDetial


app_name = 'blog_api'

urlpatterns = [
    # path('<int:pk>/', PostDetial.as_view(), name='detailcreate'),
    # path('', PostList.as_view(), name='listcreate'),
]